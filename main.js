

const config = {
    title: 'Sokoban',
    type: Phaser.AUTO,
    backgroundColor: "#192a56",
    width: 549,
    height: 480,
    render: {
        pixelArt: true,
    },
    scene: [
    ]
};

new Phaser.Game(config);
 